(defrecord Product [(id : Long) (name : String)])

(defn get-products [] : (Vec Product)
  [(->Product 1 "A") (->Product 2 "B")])

;; should fail: product-name returns String, not Long
(defn bad-names [] : (Vec Long)
  (for [p (get-products)]
    (product-name p)))
