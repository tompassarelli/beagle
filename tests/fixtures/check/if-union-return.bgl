(defrecord Product [(id : Long) (name : String)])

(declare-extern lookup-product [Long -> Product])

;; if with divergent branches returns (U Product Nil)
(defn find-product [(id : Long)] : (U Product Nil)
  (if (> id 0)
    (lookup-product id)
    nil))
