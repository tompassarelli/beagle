(defrecord Product [(id : Long) (name : String)])

(defn process-products [(products : (Vec Product))] : Long
  (count products))

;; should pass: homogeneous vec of Products
(defn good [] : Long
  (let [ps [(->Product 1 "A") (->Product 2 "B")]]
    (process-products ps)))
