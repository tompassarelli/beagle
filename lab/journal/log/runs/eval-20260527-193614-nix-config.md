# eval-roundtrip — nix-config @ e2de142

- timestamp:   2026-05-27T19:36:14+07:00
- beagle-sha:  c77a8d1
- corpus:      /tmp/nix-config
- corpus-sha:  e2de142

## twin build

| metric | count |
|---|---|
| nix files     | 233 |
| convert-fail  | 0 |
| build-fail    | 2 |

## per-host drvPath equivalence (primary signal)

| metric | count |
|---|---|
| hosts total       | 7 |
| PASS              | 0 |
| FAIL-DIVERGE      | 7 |
| FAIL-TWIN-EVAL    | 0 |
| FAIL-ORIG-EVAL    | 0 |

### FAIL-DIVERGE hosts (drvPath strings differ — real semantic divergence)

- alcyone
- atlas
- celaeno
- maia
- merope
- pleione
- taygeta

#### diagnostic-cmp reports

```
--- alcyone ---
diagnostic-cmp for host=alcyone
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- atlas ---
diagnostic-cmp for host=atlas
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- celaeno ---
diagnostic-cmp for host=celaeno
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- maia ---
diagnostic-cmp for host=maia
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- merope ---
diagnostic-cmp for host=merope
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- pleione ---
diagnostic-cmp for host=pleione
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
--- taygeta ---
diagnostic-cmp for host=taygeta
  6 MATCH / 0 DIFFER (of 6 curated leaves)
    MATCH   hostName
    MATCH   stateVersion
    MATCH   timeZone
    MATCH   kernelVersion
    MATCH   systemPackages
    MATCH   enabledSystemdServices
  VERDICT: DIVERGE-but-equivalent — all curated leaves match;
           divergence is non-user-facing (likely flake-source-hash
           cascade from self-referencing config like sops).
```

