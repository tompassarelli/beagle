#!/usr/bin/env bash
# Run beagle daemon in foreground (for systemd/process managers).
set -euo pipefail
source "$(dirname "$0")/_beagle-racket"
exec "$RACKET" -e '(require beagle/private/daemon) (run-daemon-tcp)'
