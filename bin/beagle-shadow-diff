#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/_beagle-racket"
beagle_root="$(cd "$(dirname "$0")/.." && pwd)"
exec "$RACKET" "$beagle_root/beagle-lib/private/shadow-facts-diff-cli.rkt" "$@"
