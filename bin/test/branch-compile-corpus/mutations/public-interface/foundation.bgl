#lang beagle
(ns corpus.foundation)

(defn- private-offset [(value Int)] Int
  (+ value 1))

(defn adjust [(value Int) (bonus Int)] Int
  (+ (private-offset value) bonus))

(defn double-value [(value Int)] Int
  (* value 2))
