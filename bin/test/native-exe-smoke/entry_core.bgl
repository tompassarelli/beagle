#lang beagle
(ns native.core-entry)


(def sample-values (Vec String) ["left" "right"])

(defn global-size [] Int
  (if (= sample-values []) 99 (count sample-values)))

(defn local-shadow-size [] Int
  (let [(sample-values (Vec Int)) [1 2 3]]
    (count sample-values)))
