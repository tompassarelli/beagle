#lang beagle/clj
(ns fixtures.transitive-alias-user)
(define-mode strict)

(require fixtures.transitive-alias-owner :as owner)

(defn keep-slots [(slots (Atom (Vec Int)))] Any
  (owner/->Holder slots))
