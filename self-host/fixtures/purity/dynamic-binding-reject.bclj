#lang beagle/clj
(ns selfhost.fixtures.purity.dynamic-binding-reject)
(define-mode strict)

(def ^:dynamic writer! Any
  (fn [(value Any)] Any (reset! value nil)))

(defn invoke [(replacement Any) (value Any)] Any
  (binding [writer! replacement]
    (writer! value)))
