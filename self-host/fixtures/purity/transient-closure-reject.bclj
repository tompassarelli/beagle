#lang beagle/clj
(ns selfhost.fixtures.purity.transient-closure-reject)
(define-mode strict)

(defn escape-closure [(xs Any)] Any
  (let [owned (transient xs)]
    (fn [] Any
      (conj! owned 1))))
