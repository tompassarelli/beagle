#lang beagle/clj
(ns selfhost.fixtures.purity.mixed-transient-reject)

(defn mixed [(borrowed Any) (xs Any)] Any
  (let [owned (transient xs)]
    (conj! borrowed 1)
    (conj! owned 2)
    (persistent! owned)))
