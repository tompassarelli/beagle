#lang beagle/clj
(ns selfhost.fixtures.purity.transient-alias-reject)
(define-mode strict)

(defn alias-result [(xs Any)] Any
  (let [owned (transient xs)
        alias (conj! owned 1)]
    (persistent! alias)))
