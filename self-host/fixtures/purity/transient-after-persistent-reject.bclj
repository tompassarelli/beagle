#lang beagle/clj
(ns selfhost.fixtures.purity.transient-after-persistent-reject)

(defn mutate-after-persistent [(xs Any)] Any
  (let [owned (transient xs)
        result (persistent! owned)]
    (conj! owned 1)
    result))
