#lang beagle/js
(ns selfhost.fixtures.invalid.grey-authored-any-join)

;; Authored Any is an actual public type, not the checker's inference bottom.
(defn invalid-narrowing [(value Any) (choose-value? Bool)] Int
  (+ (if choose-value? value 1) 0))
