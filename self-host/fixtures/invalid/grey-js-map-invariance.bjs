#lang beagle/js
(ns selfhost.fixtures.invalid.grey-js-map-invariance)

(defn consume-dynamic-values [(values (JsMap String Any))] Int
  (js/get values .size))

(defn invalid-map-widening [(values (JsMap String Int))] Int
  (consume-dynamic-values values))
