# generated data — regenerate with `store import`
/facts.log
*.log

/.cpcache/

.DS_Store

# Claude Code harness state (worktrees, hooks, local context)
.claude/

# Talk/strategy drafts — kept out of the public repo (anti-cringe)
private-docs/

# Internal agent notes / status / scratch / handoffs — NOT user-facing docs.
# Keep out of git. Put private notes under docs/private/.
docs/private/
__pycache__/

# direnv's realized devShell profile (.envrc)
.direnv/
