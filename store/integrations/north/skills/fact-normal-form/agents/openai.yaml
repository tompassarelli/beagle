interface:
  display_name: "Fact Normal Form"
  short_description: "Normalize and validate persisted semantic facts"
  default_prompt: "Use $fact-normal-form to normalize and validate this fact-oriented model before admission."
