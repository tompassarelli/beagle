#lang beagle/clj

;; Build-time interface for the hand-maintained out/resolve.clj entrypoint.
;; Codegraph loads that runtime namespace; this module supplies only its typed
;; surface while hosted sources are checked as a closed module graph.
(ns resolve)

(defn blast-closure [forward Any] Any nil)
