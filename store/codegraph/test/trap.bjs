#lang beagle/js
;; the red path is hot — keep red fast
(defn red [x Any] Any x)
(defn red-zone [y Any] Any (red y))
(def banner (: "red flag raised" Any))
(def exact (: "red" Any))
(defn use [] Any (red (red-zone "red")))
