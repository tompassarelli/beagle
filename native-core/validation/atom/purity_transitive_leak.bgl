#lang beagle
(ns native.atom-transitive-purity-leak)

(defn save [(cell (Atom Int)) (value Int)] Int
  (store cell value))

(defn store [(cell (Atom Int)) (value Int)] Int
  (do
    (reset! cell value)
    value))
