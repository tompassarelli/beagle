#lang beagle
(ns provider)

(defrecord Item [value Int])
(defalias Term (Dyn String Int Item))

;; Declaration order cannot override the immutable dependency edge.
(def exported-label String provider-label)
(def provider-label String "provider")
