#lang beagle
(ns native.socket-byte-sink)


(def packet-octets (Vec Int) [0 0 0 4 112 111 110 103])

(defn write-packet [(peer-fd Int)] Int
  (do
    (host.socket/write-bounded
      peer-fd
      (native.bytes/from-ints-bounded packet-octets 8)
      8)
    0))
