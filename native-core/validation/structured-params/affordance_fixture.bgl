#lang beagle
(ns native.structured-params-affordance)

(defrecord Request [(port Int)])

(defn retain-port [({:keys [port]} Request)] (Vec Int)
  (let [prefix? (str/starts-with? "port" "p")]
    [port]))
