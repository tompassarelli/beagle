# W1.4 — Drain the superseded native-decode lane

## Goal

Close and reap only the superseded native-decode lane/coordination residue associated with `native_stage_decode_gate.sh`. Preserve every currently consumed `FrozenNativeStageV0` field and every live caller of `beagle:native-core/src/native/stages.bclj`; this order does not authorize removal of the live stage encoder.

## Lane

Repository: Beagle. Create `~/code/beagle/worktrees/native-decode-drain` on branch `native-decode-drain`.

## File boundary

May edit only the superseded lane's branch/worktree disposition and the exact todo or coordination references that name `native_stage_decode_gate.sh`. The live-tree source boundary is read-only: do not edit `beagle:native-core/src/native/stages.bclj`, compiler/expander files, Store files, or gate scripts.

## Work order

1. Audit the superseded lane, branches, todo records, release receipts, and tracked references for the gate name.
2. Prove no current main consumer or release receipt depends on that lane or gate name.
3. Close the stale coordination state and reap the superseded lane through the repository's safe lane-cleanup path. Do not touch a live pin or current main.

## Verification

Record the evidence that no current main consumer or release receipt names the superseded lane. Run Native stage compilation and the current cache gate without changing their files. If a retained `FrozenNativeStageV0` caller is found, stop and report it rather than deleting a field.

## Landing bar

The stale lane and exact stale references are gone, live stage consumers are preserved, and the named Native checks pass. Commit only changed coordination documents in the new lane if there are tracked changes; land with `safe-push --to main`, fast-forward `~/code/beagle/main`, then remove both the new lane and the proven-superseded lane/branch through their safe cleanup routes.

## Staffing suggestion

Executor, economy semantic tier; suggest `gpt-5.6-terra` with low deliberation. The work is bounded residue closure with an explicit preservation rule and deterministic evidence.
