# W1.10 — Remove stale Grey temporary receipt constants

## Goal

Prove whether the Stage 4 temporary-path constants in the shadow-engine test are dead scaffolding. Remove only unused constants or replace them with fixture-local setup; retain the surrounding shadow behavior and its test value.

## Lane

Repository: Greywrought. Create `~/code/greywrought/worktrees/grey-temp-receipts` on branch `grey-temp-receipts`.

## File boundary

May edit only `greywrought:tests/shadow-engine.test.mjs` lines 21–29 and direct local test setup required by that file. Do not edit Grey production files, Stage/demo fixtures, Store files, compiler/expander files, or unrelated test infrastructure.

## Work order

1. Establish whether either `/tmp/c145bm-stage4-*.json` constant is read by the test body or setup.
2. If unused, delete the constants entirely. If the test needs a receipt, use fixture-local temporary setup owned by this file and preserve the test's observable contract.
3. Do not extend the seam into the Stage/demo migration work.

## Verification

Run the shadow-engine test with an isolated `TMPDIR`. Confirm it has no dependency on `/tmp/c145bm-stage4-real-history-receipt.json` or `/tmp/c145bm-stage4-failure-receipt.json`, then run a tracked-tree search for both removed temporary-path tokens.

## Landing bar

Land when the isolated test passes and both stale-path searches are empty. Commit the enumerated path, land with `safe-push --to main`, fast-forward `~/code/greywrought/main`, then remove the lane and branch.

## Staffing suggestion

Implementer, standard semantic tier; suggest `gpt-5.6-terra` with medium deliberation. This is a bounded test ownership repair with a focused deterministic check.
