# W1.3 — Close the lossy syntax-to-datum escape hatch

## Goal

Prove whether `beagle-syntax->datum` is unused by current compiler and supported consumers. If it is, remove the lossy legacy export and the tests that exist only for it; preserve the immutable syntax contract and its live consumers.

## Lane

Repository: Beagle. Create `~/code/beagle/worktrees/lossy-syntax` on branch `lossy-syntax`.

## File boundary

May edit only `beagle:beagle-lib/private/ast.rkt` lines 128–135 and 392–500, its export declarations, and direct tests of `beagle-syntax->datum`. Do not alter unrelated AST representation, macro implementation, parser behavior, AST JSON behavior, or self-host source.

## Work order

1. Census tracked callers of the export and distinguish retired tests/tools from live compiler or consumer use.
2. If a live caller remains, report it with the required contract and leave the escape hatch intact.
3. If callers are limited to retired tests/tools, delete the export and its sole-purpose tests completely, with no compatibility shim.

## Verification

Run the tracked caller census, syntax and macro focused gates, self-host remint, and the AST JSON gate. After deletion, search the tracked tree for `beagle-syntax->datum`; it must have no result.

## Landing bar

Land only with a zero-live-caller proof, passing named checks, and an empty removed-export search. Commit the enumerated paths, land with `safe-push --to main`, fast-forward `~/code/beagle/main`, then remove the lane and branch.

## Staffing suggestion

Integrator, senior semantic tier; suggest `gpt-5.6-sol` with high deliberation. The change is small but owns an AST/macro compatibility boundary.
