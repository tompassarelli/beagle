# W1.2 — Remove retired compiler dispatch only after caller proof

## Goal

Remove retired nominal/JVM and legacy parse-dispatch vocabulary only where the caller census establishes that no current consumer requires it. The supported compiler must retain its current parser, checker, emitter, and self-host behavior; this is not a syntax redesign.

## Lane

Repository: Beagle. Create `~/code/beagle/worktrees/legacy-dispatch` on branch `legacy-dispatch`.

## File boundary

May edit only `beagle:beagle-lib/private/parse.rkt` at the legacy arms around lines 1111 and 2924–3347, `beagle:beagle-lib/private/emit-js.rkt` at the legacy-name construction around lines 1301–1334, and direct tests/fixtures that exclusively exercise removed forms. Do not change unrelated parser forms, checker rules, emitters, self-host sources, or tier infrastructure.

## Work order

1. Use `bin/beagle callers` and tracked references to classify every current caller of each candidate legacy arm.
2. If a live caller remains, stop and report the caller and retained contract; do not infer a replacement.
3. If no live caller remains, delete the entire retired dispatch surface and its direct tests. Do not leave error-only shims or legacy aliases.

## Verification

Run the compiler caller query, the parser/checker/emitter active tier, self-host remint, and the exact negative fixtures for the removed legacy forms. Finish with a tracked-tree token search for each removed legacy name.

## Landing bar

Only land a deletion backed by a zero-live-caller census, passing named focused checks, and an empty removed-name search. Commit the enumerated paths, land with `safe-push --to main`, fast-forward `~/code/beagle/main`, then remove the lane and branch.

## Staffing suggestion

Integrator, senior semantic tier; suggest `gpt-5.6-sol` with xhigh deliberation. This is a foundational compiler compatibility decision with several independently observable outputs.
