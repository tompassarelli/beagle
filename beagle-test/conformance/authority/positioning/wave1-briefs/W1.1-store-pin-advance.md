# W1.1 — Advance the Beagle Store compiler pin

## Goal

Advance the Store compiler pin as one coherent consumer cutover, then remove every tracked reference to `b4f3081420a3be73d730802d2f4608d78d0c6cf4`. Do not treat `beagle:store/beagle-pin.txt` as an isolated version bump: the pin policy requires its flake input/lock, CI readers, and license record to move together.

## Lane

Repository: Beagle. Create `~/code/beagle/worktrees/store-pin-advance` on branch `store-pin-advance`.

## File boundary

May edit only `beagle:store/beagle-pin.txt`, `beagle:store/AGENTS.md`, and the exact tracked Store flake, lock, CI, and license readers of the old object found by the initial census. Do not alter compiler, expander, or unrelated Store code. Do not advance a named consumer without including it in this lane.

## Work order

1. Enumerate tracked readers of the exact old object and identify the intended replacement compiler object and every Store consumer that must move with it.
2. Advance the complete declared pin surface atomically. Preserve the policy record with the new exact object and its license provenance.
3. Remove the old-object surface rather than leaving a compatibility reference.

## Verification

Run the Store native build-cache gate and the native/Cloudflare release-artifact gates named by the Store pin policy. Run a tracked-tree search for the exact old object; it must return zero results after the cutover. Report the chosen replacement object and the readers found by the census.

## Landing bar

The named checks pass once for the lane commit, the exact-object search is empty, and the patch contains every declared reader. Commit the enumerated paths, land with `safe-push --to main`, fast-forward `~/code/beagle/main`, then remove the lane and branch.

## Staffing suggestion

Integrator, senior semantic tier; suggest `gpt-5.6-sol` with high deliberation. The boundary is narrow, but a mistaken compiler consumer cutover has release-artifact blast radius.
