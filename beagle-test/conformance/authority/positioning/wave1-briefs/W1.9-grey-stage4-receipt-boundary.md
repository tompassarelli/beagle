# W1.9 — Resolve the Grey historical receipt migration boundary

## Goal

Replace the Stage 4 fixtures only after a current historical cutover receipt contract, or an explicitly bounded migration reader, proves the same matched/diverged history behavior. Do not delete migration evidence merely because its path is named `stage-4`.

## Lane

Repository: Greywrought. Create `~/code/greywrought/worktrees/cutover-receipts` on branch `cutover-receipts`.

## File boundary

May edit only `greywrought:tools/stage-5-demo.mjs` lines 20–32 and 337–370, `greywrought:tests/fixtures/stage-4/*`, and the direct Stage 5 acceptance fixtures/tests needed to establish the retained or replacement contract. Do not alter authority runtime, Store codec, browser, Native, deployment, or unrelated Grey test files.

## Work order

1. Determine whether the parked playable redeploy has already superseded the harness. If yes, remove the entire obsolete harness/fixtures surface only where its current caller graph is empty.
2. Otherwise define the smallest semantic current receipt boundary that proves the existing matched/diverged history contract, or retain an explicit bounded migration reader.
3. Migrate direct callers and remove the old fixtures, hashes, and tests only after the replacement proof exists. Do not create a Stage-name shim.

## Verification

Run `bun test acceptance/stage-5-demo.test.mjs` (or its semantically renamed direct replacement). Verify the same matched/diverged history contract is covered. After any removal, run a tracked-tree search for each removed Stage 4 fixture/hash token.

## Landing bar

Land only when the replacement or bounded-reader decision is explicit, the direct acceptance proof passes, and every removed fixture/hash has an empty tracked-tree search. Commit the enumerated paths, land with `safe-push --to main`, fast-forward `~/code/greywrought/main`, then remove the lane and branch.

## Staffing suggestion

Designer, frontier semantic tier; suggest `gpt-5.6-sol` with xhigh deliberation. The worker must choose a durable migration-contract boundary, not just perform a mechanical fixture deletion.
