# W1.5 — Burn down reintroduced qualified-reference scaffold

## Goal

Use the scaffold guard and caller evidence to remove any genuinely reintroduced W1–W4 accessor/bridge residue while preserving the live structural `qualified-ref` representation. This is a targeted absence proof, not authorization to delete the representation.

## Lane

Repository: Beagle. Create `~/code/beagle/worktrees/scaffold-guard` on branch `scaffold-guard`.

## File boundary

May edit only `beagle:bin/test/qualified-ref-scaffold/run.sh`, the structural-name bridge callsites in `beagle:beagle-lib/private/ast.rkt` around lines 138–220 and 664–712, and direct tests/fixtures for an identified dead accessor or old spelling. Do not change `qualified-ref` representation semantics, parser dispatch, Store code, or unrelated test gates.

## Work order

1. Run the guard and a tracked search for the old accessor and spelling.
2. Classify every nonzero result as live structural representation, necessary bridge, or reintroduced dead scaffold.
3. Delete only the third category with its direct tests. A live or ambiguous result blocks deletion and must be reported with its caller.

## Verification

The guard must report zero reintroduced scaffold, branch-corpus identity and rename gates must pass, and tracked source must contain no removed accessor token. Preserve evidence that the remaining `qualified-ref` representation is live.

## Landing bar

Land only an identified dead-surface deletion with passing named checks and an empty removed-token search. Commit the enumerated paths, land with `safe-push --to main`, fast-forward `~/code/beagle/main`, then remove the lane and branch.

## Staffing suggestion

Integrator, senior semantic tier; suggest `gpt-5.6-sol` with high deliberation. The distinction between live structural state and dead bridge vocabulary is a compiler-boundary judgment.
