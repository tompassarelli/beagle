// generated by beagle (zig backend) — do not edit
const std = @import("std");
const rt = @import("beagle_rt.zig");
pub const Ctx = rt.Ctx;

pub const DIGIT: rt.Regex = rt.regex("[0-9]");

pub const VALUES: []const []const u8 = &.{ "x" };

pub fn mapValue(record: rt.ValueMap(rt.Keyword, []const u8)) bool {
    return rt.is_map(record);
}

pub fn __beagle_main(__ctx: *rt.Ctx) void {
    _ = rt.println(rt.str1(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str2(__ctx.tick, rt.str1(__ctx.tick, rt.truthy(rt.re_matches0(DIGIT, "7"))), rt.str1(__ctx.tick, ":")), rt.str1(__ctx.tick, rt.is_string("fram"))), rt.str1(__ctx.tick, ":")), rt.str1(__ctx.tick, mapValue(rt.ValueMap(rt.Keyword, []const u8).empty(__ctx.tick).assoc(__ctx.tick, rt.keyword("", "name"), "fram")))), rt.str1(__ctx.tick, ":")), rt.str1(__ctx.tick, rt.is_sequential(VALUES))), rt.str1(__ctx.tick, ":")), rt.str1(__ctx.tick, rt.is_int(7))), rt.str1(__ctx.tick, ":")), rt.str1(__ctx.tick, rt.is_int(7)))));
}

pub fn main() void {
    var __arena = std.heap.ArenaAllocator.init(std.heap.page_allocator);
    defer __arena.deinit();
    var __rng = rt.Splitmix64.init(0);
    var __ctx = rt.Ctx{
        .tick = __arena.allocator(),
        .rng = &__rng,
    };
    __beagle_main(&__ctx);
}
