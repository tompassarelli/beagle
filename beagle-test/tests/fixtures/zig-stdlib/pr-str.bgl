(ns zig-stdlib.pr-str)

(defn render
  [record :- (Map Keyword (Dyn String Int))]
  :- String
  (pr-str record))

(defn main [] :- Nil
  (println
    (render
      {:name "fram"
       :version 7})))
