#lang beagle/js
;; @error[bad-form] (defn check ...) — `check` is a reserved built-in form name and cannot be redefined; calls to it would resolve to the built-in, not your function. Rename the function.
(ns app.checkbug)
(define-mode strict)
(defn check [(x Int)] Int (+ x 100))
