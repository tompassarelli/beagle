#lang beagle/nix
(ns test.nix.rec-assert)

(nix/module [config lib]
  {:networking (rec-attrs hostName "myhost"
                          domain ~"${hostName}.local")
   :boot (nix/assert config.boot.isContainer
           {:kernelModules ["kvm-amd"]})})
