(defunion (Result T E)
  (Ok [(value : T)])
  (Err [(error : E)]))

(defrecord Order [(id : Int) (name : String)])
(defrecord OrderError [(code : Int) (message : String)])

(defn handle [(r : (Result Order OrderError))] : String
  (match r
    [(Ok order) (order-name order)]
    [(Err e) (ordererror-message e)]))
