(defrecord Product [(id : Int) (name : String) (price : Int)])

(defn get-product [] :- Product
  (->Product 1 "Widget" 500))

;; destructuring should propagate field types from Product
(defn use-name [] :- String
  (let [{:keys [name]} (get-product)]
    name))
