(defrecord Product [(id : Int) (name : String)])

(declare-extern lookup-product [Int -> Product])

;; should fail: if returns (U Product Nil), not Product
(defn find-product [(id : Int)] :- Product
  (if (> id 0)
    (lookup-product id)
    nil))
