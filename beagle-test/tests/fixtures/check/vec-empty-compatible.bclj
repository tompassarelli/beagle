(defrecord Product [(id : Int)])

(claim process-products [(Vec Product) -> Int])
(defn process-products [(ps : (Vec Product))]
  (count ps))

;; empty vec is (Vec Any), compatible with any (Vec T)
(claim ok [-> Int])
(defn ok []
  (process-products []))
