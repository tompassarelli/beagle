(defrecord Product [(id : Int) (name : String)])

(declare-extern lookup-product [Int -> Product])

;; if with divergent branches returns (U Product Nil)
(defn find-product [(id : Int)] :- (U Product Nil)
  (if (> id 0)
    (lookup-product id)
    nil))
