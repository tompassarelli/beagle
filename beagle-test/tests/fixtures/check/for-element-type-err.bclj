(defrecord Product [(id : Int) (name : String)])

(defn get-products [] :- (Vec Product)
  [(->Product 1 "A") (->Product 2 "B")])

;; should fail: product-name returns String, not Int
(defn bad-names [] :- (Vec Int)
  (for [p (get-products)]
    (product-name p)))
