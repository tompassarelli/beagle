#lang beagle/clj
(ns zig-imported-record-types.types)
(define-mode strict)

(defrecord Store [value :- Int])

(defn make-store [value :- Int] :- Store
  (->Store value))

(defn store-value [store :- Store] :- Int
  (:value store))
