#lang beagle/js
(ns f0.callables.arity-overlap)

;; Invalid: both clauses accept exactly one argument.  The parser must reject
;; this before a backend can make dispatch depend on generated function shape.
(def ambiguous Any
  (fn
    ([value Int] Int value)
    ([first Int & more (List Int)] Int
     (+ first (count more)))))
