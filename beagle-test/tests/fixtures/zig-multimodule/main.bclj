#lang beagle/clj
(ns zig-multimodule.main
  (:require [zig-multimodule.bridge :as bridge]))
(define-mode strict)

(defn main [] -> Nil
  (println
   (if (bridge/before? "alpha" "beta")
     "multi-module zig ok"
     "multi-module zig failed")))
