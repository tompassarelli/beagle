#lang beagle/sql
(ns test.sql.delete)

(sql/deftable sessions
  [(id         : Int :primary-key)
   (expires_at : String)
   (revoked    : Bool)])

;; DELETE with WHERE.
(sql/delete sessions
  (where (or (< expires_at "2024-01-01")
             (= revoked #t))))
