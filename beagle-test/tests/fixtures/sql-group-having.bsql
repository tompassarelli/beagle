#lang beagle/sql
(ns test.sql.group-having)

;; GROUP BY + HAVING with multiple aggregates.
(sql/select [category (count id :as item_count) (avg price :as avg_price)]
  (from products)
  (group-by category)
  (having (> (count id) 5))
  (order-by avg_price :desc)
  (limit 10))
