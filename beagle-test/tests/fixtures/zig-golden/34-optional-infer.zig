// generated by beagle (zig backend) — do not edit
const std = @import("std");
const rt = @import("beagle_rt.zig");
const los_rt = @import("los_rt.zig");
pub const Ctx = rt.Ctx;

pub fn resolveOr(key: []const u8, fallback: []const u8) []const u8 {
    const hit = los_rt.lookup(key);
    return (if ((hit != null)) hit.? else fallback);
}

pub fn parsedOrZero(s: []const u8) i64 {
    const n = rt.parse_long(s);
    return (if ((n != null)) n.? else 0);
}

pub fn countOrDefault(m: rt.Map(i64), k: []const u8, fallback: i64) i64 {
    const v = m.get(k);
    return (if ((v == null)) fallback else v.?);
}
