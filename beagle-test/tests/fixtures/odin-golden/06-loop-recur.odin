// generated by beagle (odin backend) — do not edit
package g


sum_to :: proc(n: int) -> int {
    i := 0;
    acc := 0;
    for {
        if (i >= n) {
            return acc;
        } else {
            _r0 := (i + 1);
            _r1 := (acc + i);
            i = _r0;
            acc = _r1;
            continue;
        }
    }
}
