// generated by beagle (odin backend) — do not edit
package imk


CKEY_OFFSET: int : 2000000;

CKEY_STRIDE: int : 4000001;

chunk_key :: proc(cx: int, cz: int) -> int {
    return (((cx + CKEY_OFFSET) * CKEY_STRIDE) + (cz + CKEY_OFFSET));
}

chunk_cx :: proc(key: int) -> int {
    return ((key / CKEY_STRIDE) - CKEY_OFFSET);
}

Store :: struct {
    cells: map[int]int,
}

put :: proc(s: ^Store, cx: int, cz: int, v: int) {
    s^.cells[chunk_key(cx, cz)] = v;
}

has :: proc(s: ^Store, cx: int, cz: int) -> bool {
    return (chunk_key(cx, cz) in s^.cells);
}
