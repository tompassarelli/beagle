// generated by beagle (zig backend) — do not edit
const std = @import("std");
const rt = @import("beagle_rt.zig");
pub const Ctx = rt.Ctx;

pub const NO_CAPTURE: rt.Regex = rt.regex("c.t");

pub const OPTIONAL_CAPTURE: rt.Regex = rt.regex("^(a)?b$");

pub const MULTIPLE_CAPTURES: rt.Regex = rt.regex("^([a-z]+)-([0-9]+)$");

pub const ESCAPED_GROUP: rt.Regex = rt.regex("^\\(x\\)$");

pub const REPLACE_RUN: rt.Regex = rt.regex("[^a-z0-9]+");

pub const SPLIT_RUN: rt.Regex = rt.regex("[,;]+");

pub fn findNoCapture(s: []const u8) ?[]const u8 {
    return rt.re_find0(NO_CAPTURE, s);
}

pub fn matchOptionalCapture(s: []const u8) ?rt.RegexMatch(2) {
    return rt.re_matches(1, OPTIONAL_CAPTURE, s);
}

pub fn matchMultipleCaptures(s: []const u8) ?rt.RegexMatch(3) {
    return rt.re_matches(2, MULTIPLE_CAPTURES, s);
}

pub fn matchEscapedGroup(s: []const u8) ?[]const u8 {
    return rt.re_matches0(ESCAPED_GROUP, s);
}

pub fn replaceRuns(__ctx: *rt.Ctx, s: []const u8) []const u8 {
    return rt.regex_replace(__ctx.tick, s, REPLACE_RUN, "_");
}

pub fn splitRuns(__ctx: *rt.Ctx, s: []const u8) []const []const u8 {
    return rt.regex_split(__ctx.tick, s, SPLIT_RUN);
}
