# Rename-gate scratch fixtures

Manual `.clj` inputs used while developing the rename/binding-scope gates
(rename gates 16–17: `as->`/`letfn`/`extend-type` impl binding scopes, etc.).
They are **not** wired into the automated test suite — they're hand-run scratch
inputs, preserved here (moved out of the repo root, 2026-06-20) for reference.
If any becomes worth automating, promote it into an oracle/structure test.
